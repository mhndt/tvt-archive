# Developer/test override. Normal users should pull the published GHCR image
# with compose/compose.yaml and should not build locally.
services:
  tvt-archive:
    image: ${TVT_ARCHIVE_LOCAL_IMAGE:-tvt-archive:0.8.2-local}
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        APP_VERSION: "0.8.2"
